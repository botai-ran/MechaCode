# 2026-07-06 已完成功能清单

> 本清单根据当前代码仓库状态整理，只记录已经落地的功能。

## 工程基础

- [x] 搭建 `pnpm workspace` monorepo 工程结构。
- [x] 建立根目录统一脚本：`dev`、`build`、`check`、`lint`、`typecheck`、`test`。
- [x] 配置 Node.js 22、pnpm 11、TypeScript 5 的基础开发环境约束。
- [x] 建立 `apps/desktop` 桌面端应用目录。
- [x] 建立 `packages/protocol`、`packages/agent-runtime`、`packages/agent-tools`、`packages/shared` 基础包。
- [x] 配置 ESLint、TypeScript base config、workspace package 依赖关系。

## 桌面端应用

- [x] 搭建 Tauri 2 + React 18 + Vite 桌面端应用。
- [x] 实现基础桌面聊天界面，包括会话侧边栏、消息列表、输入框和发送按钮。
- [x] 实现会话切换的前端状态管理。
- [x] 实现 provider 选择控件，支持 OpenAI、Anthropic、DeepSeek 的可用状态展示。
- [x] 实现消息发送 loading 状态和错误提示。
- [x] 支持 `Enter` 发送消息、`Shift + Enter` 换行。
- [x] 前端通过 Tauri `invoke` 调用后端命令获取 Agent 配置。
- [x] 前端通过 Tauri `invoke` 调用后端命令发起 Agent 对话。
- [x] 接入基础 SCSS 样式拆分，包括 base、layout、chat、tokens。

## Tauri 后端桥接

- [x] 实现 `get_agent_config` Tauri command。
- [x] 实现 `run_agent_chat` Tauri command。
- [x] 后端可从系统环境变量和仓库 `.env` 判断可用模型服务商。
- [x] 后端可选择默认 provider。
- [x] 后端可校验 provider 合法性和消息列表有效性。
- [x] 后端可通过 `pnpm --filter @mecha/agent-runtime chat` 调用 Agent Runtime。
- [x] 后端支持将前端消息序列化为 base64 JSON 传入 CLI。
- [x] 后端支持把 Agent Runtime 输出返回给桌面端。
- [x] 接入 `tauri-plugin-opener`。

## Agent Runtime

- [x] 实现 `ChatRuntime` 统一聊天入口。
- [x] 支持根据 provider 创建对应模型服务商适配器。
- [x] 支持运行时默认模型配置。
- [x] 支持单次请求通过 `input.model` 覆盖默认模型。
- [x] 支持非流式聊天调用：`runtime.chat(input)`。
- [x] 支持流式聊天调用：`runtime.streamChat(input)`。
- [x] 统一流式输出事件为 `text_delta` 和 `done`。
- [x] 定义统一聊天类型：`ChatMessage`、`ChatInput`、`ChatOutput`、`ChatStreamEvent`、`ModelProvider`。
- [x] 定义 provider 标识：`openai`、`anthropic`、`deepseek`。
- [x] 定义模型配置错误类型 `ProviderConfigError`。

## 模型服务商适配

- [x] 接入 OpenAI 官方 SDK。
- [x] OpenAI provider 支持 Responses API 的非流式调用。
- [x] OpenAI provider 支持 Responses API 的流式调用。
- [x] OpenAI provider 支持读取 `OPENAI_API_KEY` 和 `OPENAI_MODEL`。
- [x] OpenAI provider 支持把 system message 转为 `instructions`。
- [x] 接入 Anthropic 官方 SDK。
- [x] Anthropic provider 支持 Messages API 的非流式调用。
- [x] Anthropic provider 支持 Messages API 的流式调用。
- [x] Anthropic provider 支持读取 `ANTHROPIC_API_KEY` 和 `ANTHROPIC_MODEL`。
- [x] Anthropic provider 支持统一消息格式到 Anthropic messages 的转换。
- [x] 接入 DeepSeek OpenAI-compatible API。
- [x] DeepSeek provider 支持非流式聊天调用。
- [x] DeepSeek provider 支持流式聊天调用。
- [x] DeepSeek provider 支持读取 `DEEPSEEK_API_KEY`、`DEEPSEEK_MODEL`、`DEEPSEEK_BASE_URL`。
- [x] DeepSeek provider 默认 base URL 为 `https://api.deepseek.com`。

## CLI 对话能力

- [x] 为 `@mecha/agent-runtime` 提供 `mecha-chat` bin 入口。
- [x] 实现 `pnpm --filter @mecha/agent-runtime chat` 命令。
- [x] CLI 支持 `--provider openai|anthropic|deepseek`。
- [x] CLI 支持 `--model <model>`。
- [x] CLI 支持 `--no-stream`。
- [x] CLI 支持 `--no-tools`。
- [x] CLI 支持直接从命令行传入 prompt。
- [x] CLI 支持未传入 prompt 时从终端交互读取输入。
- [x] CLI 支持从 `--messages-json` 传入完整消息数组。
- [x] CLI 支持从 `--messages-json-base64` 传入完整消息数组。
- [x] CLI 默认启用流式输出。
- [x] CLI 可从当前目录或仓库根目录加载 `.env`。
- [x] CLI 内置中文回复风格的基础 system prompt。
- [x] CLI 对模型连接类错误支持有限次数自动重试。

## 工具调用能力

- [x] 定义 `AgentTool` 工具接口。
- [x] 定义工具 `inputSchema` 结构，用于暴露给模型。
- [x] 实现 `ToolRegistry` 工具注册表。
- [x] 支持注册单个工具。
- [x] 支持批量注册工具。
- [x] 支持按名称查找工具。
- [x] 支持列出当前已注册工具。
- [x] 实现默认工作区工具集合 `createWorkspaceTools`。
- [x] 实现默认工具注册表 `createDefaultToolRegistry`。
- [x] CLI 已接入 DeepSeek 工具调用循环。
- [x] 工具调用循环支持最多 6 轮工具执行。
- [x] 工具调用过程会输出用户可读的进度信息。
- [x] 工具调用结果会作为 tool message 回传给模型。

## 工作区工具

- [x] 实现 `read_file`：读取工作区内文本文件。
- [x] 实现 `write_file`：写入工作区内文本文件并返回大小和 sha256。
- [x] 实现 `list_dir`：列出工作区目录子项。
- [x] 实现 `search_text`：递归全文搜索工作区文本内容。
- [x] 实现 `apply_patch`：通过 `git apply` 校验或应用 unified diff。
- [x] 实现 `run_command`：在工作区内执行命令并返回 stdout、stderr、退出码。
- [x] 实现 `git_diff`：读取工作区或 staged diff。
- [x] 实现 `git_status`：读取 Git 仓库状态。
- [x] 工作区路径解析会限制目标位于 workspace 内。
- [x] 命令执行支持超时和输出截断。
- [x] 文本读取和搜索支持最大结果或最大字节数限制。

## 协议类型

- [x] 定义 `AgentRole`：`system`、`user`、`assistant`、`tool`。
- [x] 定义 `AgentMessage`，包含消息 ID、角色、正文和创建时间。
- [x] 定义 `ToolCall`，包含调用 ID、工具名称和输入。
- [x] 定义 `RunEvent` 事件类型。
- [x] `RunEvent` 支持 `message`、`tool_call`、`tool_result`、`error`。

## 共享能力与文档

- [x] `packages/shared` 实现轻量级 `Result<T, E>` 类型。
- [x] `packages/shared` 提供 `ok(value)` 和 `err(error)` 辅助函数。
- [x] 建立项目 README，记录技术栈、环境要求、项目结构和常用命令。
- [x] 建立 docs 文档目录。
- [x] 建立桌面端架构文档。
- [x] 建立 UI 样式开发规范文档。
- [x] 建立代码语言规范文档。
