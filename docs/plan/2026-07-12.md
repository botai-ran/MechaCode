# 2026-07-06 至 2026-07-12 每日开发计划

> 本周目标：把 Mecha Agent 从“桌面聊天原型 + CLI 工具调用”推进到“桌面端可观察的 Agent 事件流雏形”，并为后续文件编辑、diff 预览和权限确认打好基础。

## 2026-07-06：确定方向与事件协议草案

目标：把本周开发范围定下来，先明确 Agent 运行时和桌面端之间要传递什么事件。

- [x] 完成本周开发计划文档。
- [x] 梳理当前已实现能力和缺口。
- [x] 设计 `AgentRunEvent` 事件协议草案。
- [x] 明确事件流至少需要覆盖模型请求中、模型输出、工具调用、工具结果、错误和完成状态。
- [x] 确认 Tauri 到前端的事件传递方案，优先使用 Tauri event emit。

事件协议草案：

```ts
type AgentRunEvent =
  | { type: "run_start"; runId: string }
  | { type: "model_request_start"; runId: string }
  | { type: "message_start"; messageId: string; role: "assistant" }
  | { type: "text_delta"; messageId: string; text: string }
  | { type: "message_done"; messageId: string }
  | { type: "tool_call_start"; toolCallId: string; name: string; input: unknown }
  | { type: "tool_call_done"; toolCallId: string }
  | { type: "tool_result"; toolCallId: string; output: unknown }
  | { type: "error"; message: string }
  | { type: "run_done"; runId: string };
```

事件含义：

- [x] `run_start`：一次 Agent run 开始。
- [x] `model_request_start`：开始请求模型接口，桌面端显示“思考中”。
- [x] `message_start`：开始生成一条 assistant 消息，前端创建空消息占位。
- [x] `text_delta`：模型返回一段增量文本，前端追加到对应 assistant 消息。
- [x] `message_done`：当前 assistant 消息生成完成。
- [x] `tool_call_start`：模型请求调用工具。
- [x] `tool_call_done`：工具调用动作结束。
- [x] `tool_result`：工具执行结果回传。
- [x] `error`：运行过程中出现错误。
- [x] `run_done`：一次 Agent run 完成。

## 2026-07-07：实现 Agent 事件协议与基础 run 入口

目标：让 `agent-runtime` 可以用事件流方式完成一次普通聊天。

- [x] 在 `packages/protocol` 中新增或扩展 `AgentRunEvent` 类型。
- [x] 定义基础事件类型：
  - [x] `run_start`
  - [x] `model_request_start`
  - [x] `message_start`
  - [x] `text_delta`
  - [x] `message_done`
  - [x] `error`
  - [x] `run_done`
- [x] 在 `packages/agent-runtime` 中新增 Agent run 入口。
- [x] Agent run 入口支持普通聊天，不要求当天支持工具调用。
- [x] 请求模型接口前先发出 `model_request_start`，桌面端用它展示“思考中”。
- [x] 将 provider 的 `streamChat` 输出转换为统一 `AgentRunEvent`。
- [x] 处理模型异常，把异常转换成 `error` 事件。

## 2026-07-08：把工具调用循环迁移到 runtime 层

目标：让工具调用不再只属于 CLI，而是成为 `agent-runtime` 的能力。

- [x] 在 runtime 层重新实现 provider-agnostic 工具调用循环。
- [x] 在 runtime 层实现工具调用模式。
- [x] 将工具调用过程转换为事件：
  - [x] `tool_call_start`
  - [x] `tool_call_done`
  - [x] `tool_result`
- [x] 工具执行失败时返回工具错误结果，不直接中断整个 run。
- [x] 保留最大工具调用轮数限制。
- [x] CLI 改为调用 runtime 层工具循环。
- [x] 保持 `--no-tools` 行为可用。

## 2026-07-09：接入桌面端流式 Agent command

目标：让桌面端能实时收到 Agent run 事件，而不是等待一次性响应。

- [x] 新增 Tauri command：`start_agent_run`。
- [x] 在 Tauri 后端启动 Agent run 子流程。
- [x] 将 runtime 或 CLI 产生的事件转发给前端。
- [x] 前端新增事件监听逻辑。
- [x] 发送用户消息后立即创建 assistant 占位消息。
- [x] 收到 `text_delta` 时追加到当前 assistant 消息。
- [x] 收到 `run_done` 时结束发送状态。
- [x] 收到 `error` 时展示错误并结束发送状态。
- [x] 保留现有 `run_agent_chat` 非流式接口作为回退。

## 2026-07-10：展示工具调用过程与运行状态

目标：让用户在桌面端看得见 Agent 正在做什么。

- [ ] 在前端消息流中新增工具事件展示结构。
- [ ] 展示工具名称、执行中状态、完成状态和失败状态。
- [ ] 为 `read_file`、`list_dir`、`search_text` 增加中文摘要展示。
- [ ] 为 `run_command` 展示命令、退出码和截断输出摘要。
- [ ] 为 `git_diff` 和 `git_status` 展示简短结果。
- [ ] 前端顶部或输入区展示当前运行状态：
  - [ ] 正在思考
  - [ ] 正在调用工具
  - [ ] 正在生成回复
  - [ ] 已完成
  - [ ] 出错
- [ ] 工具失败时以可读方式显示，并允许模型继续总结。

当天验收：

- [ ] 桌面端至少能展示一种工具调用过程，例如 `search_text` 或 `read_file`。
- [ ] 工具调用事件可以从“执行中”更新为“已完成”或“失败”。
- [ ] 用户能从界面看出当前 Agent 不是卡住，而是在执行具体步骤。

## 2026-07-11：整理会话状态、workspace 与权限标记

目标：让桌面端从 mock 会话推进到更真实的本地会话状态，并补上工作区上下文。

- [ ] 重构前端会话状态结构。
- [ ] 实现“新建会话”按钮的真实行为。
- [ ] 新建会话后清空消息并切换到新会话。
- [ ] 切换会话时保留不同会话的消息记录。
- [ ] 会话标题先用首条用户消息自动生成。
- [ ] 发送中禁止切换 provider，避免一次 run 中配置漂移。
- [ ] 明确当前 workspace root 的来源。
- [ ] 在桌面端展示当前 workspace 路径。
- [ ] 在 Agent run 请求中显式传入 workspace root。
- [ ] 为工具增加权限分类字段：
  - [ ] `read`：读文件、列目录、搜索、读 git 状态。
  - [ ] `write`：写文件、应用 patch。
  - [ ] `command`：执行命令。
- [ ] 本周只展示权限信息，不实现完整确认弹窗。

当天验收：

- [ ] 点击“新建会话”会创建真实新会话。
- [ ] 切换会话不会丢失各自消息。
- [ ] 桌面端能显示当前 workspace 路径。
- [ ] 工具事件或工具定义中能看出权限分类。

## 2026-07-12：收尾、测试与文档

目标：跑通一条端到端流程，把本周成果整理到可继续开发的状态。

- [ ] 跑通桌面端端到端流程：发送消息、实时输出、展示工具调用、完成回复。
- [ ] 跑通 CLI 普通聊天流程。
- [ ] 跑通 CLI 工具调用流程。
- [ ] 补充 workspace path validation 测试。
- [ ] 补充工具 registry 测试。
- [ ] 补充 provider message 转换测试。
- [ ] 补充 README 中 `.env` 与桌面端运行说明。
- [ ] 更新相关架构文档，记录 Agent event flow。
- [ ] 运行 `pnpm typecheck`。
- [ ] 运行 `pnpm lint`。
- [ ] 汇总本周完成项。
- [ ] 为下一周计划列出候选任务。

当天验收：

- [ ] 桌面端可以实时展示模型输出。
- [ ] 桌面端可以展示至少一种工具调用过程。
- [ ] OpenAI-compatible provider 可以通过 runtime 层执行只读工具。
- [ ] CLI 原有能力没有回退。
- [ ] 工具路径限制测试通过。
- [ ] `pnpm typecheck` 通过。
- [ ] 本周新增能力已写入文档。

## 本周最终验收标准

- [ ] 桌面端发送消息后，可以实时看到模型文本逐步输出。
- [ ] 至少 OpenAI-compatible provider 可以通过 runtime 层工具循环执行只读工具。
- [ ] 桌面端可以展示至少一种工具调用过程，例如 `search_text` 或 `read_file`。
- [ ] 桌面端支持真实新建会话和会话切换。
- [ ] 桌面端可以展示当前 workspace 路径。
- [ ] 工具具备初步权限分类。
- [ ] CLI 仍然支持原有聊天能力。
- [ ] 工具路径限制测试通过。
- [ ] 项目通过 `pnpm typecheck`。
- [ ] 本周新增能力有对应文档记录。

## 风险与处理

- [ ] 如果 Tauri 事件推送实现成本高，先用一次 run 一个事件日志文件或简单轮询方案过渡。
- [ ] 如果工具调用迁移影响 CLI 稳定性，先保留 CLI 原逻辑，再引入 runtime 新入口并逐步切换。
- [ ] 如果流式工具调用和 provider 差异过大，本周先以 OpenAI-compatible 路径跑通闭环，Anthropic 保持普通聊天。
- [ ] 如果测试框架缺口较大，先补核心纯函数测试，避免被端到端环境卡住。
